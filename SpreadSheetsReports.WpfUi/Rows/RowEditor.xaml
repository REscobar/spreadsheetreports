<UserControl x:Class="SpreadSheetsReports.WpfUi.Rows.RowEditor"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SpreadSheetsReports.WpfUi.Rows"
             xmlns:cells="clr-namespace:SpreadSheetsReports.WpfUi.Cells"
             xmlns:e="clr-namespace:SpreadSheetsReports.WpfUi"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <UserControl.Resources>
        <e:DebuggerConverter x:Key="Debugger"/>
    </UserControl.Resources>
    <ListView ItemsSource="{Binding Cells}" BorderBrush="{x:Null}" Background="{x:Null}" ScrollViewer.VerticalScrollBarVisibility="Auto">
        <ListView.ItemsPanel>
            <ItemsPanelTemplate>
                <StackPanel Orientation="Horizontal"/>
            </ItemsPanelTemplate>
        </ListView.ItemsPanel>
        <ListView.ItemTemplate>
            <DataTemplate>
                <cells:Cell Margin="-5,-2,-5,-2" Width="40" Padding="0,0,0,0" />
                
            </DataTemplate>
        </ListView.ItemTemplate>
    </ListView>
</UserControl>
